import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Paper, IconButton } from '@mui/meterial';
import { SearchIcon } from '@mui/icons';

const searchBar = () => {
  return (
    <div>searchBar</div>
  )
}

export default searchBar